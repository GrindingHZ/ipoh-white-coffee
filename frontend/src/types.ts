export interface AmbientCondition {
  icon: string;
  label: string;
  value: string;
  sub: string;
}

export interface Wave {
  r: number;
}

export interface Burst {
  r: number;
  alpha: number;
}
